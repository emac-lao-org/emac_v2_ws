mutation deleteWTUser($id: String, $verification_status: Int = 3) {
  update_emac_wt_users(where: {id: {_eq: $id}}, _set: { verification_status: $verification_status}) {
    returning {
      updated_at
    }
  }
}