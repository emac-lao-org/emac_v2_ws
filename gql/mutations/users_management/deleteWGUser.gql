mutation deleteWGUser($id: String, $verification_status: Int = 3) {
  update_emac_wg_users(where: {id: {_eq: $id}}, _set: { verification_status: $verification_status}) {
    returning {
      updated_at
    }
  }
}